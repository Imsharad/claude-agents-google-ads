[project]
name = "google-ads-automation-agent"
version = "0.1.0"
description = "A Google Ads automation agent."
requires-python = ">=3.9"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
